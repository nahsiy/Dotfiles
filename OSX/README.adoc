= Configuration terminal macOS

Le guide d'installation est dans le link:../README.md[README principal].

[source,shell]
----
# Depuis la racine du dépôt : aperçu sans modifier les dotfiles
bash OSX/install.sh --check
----

Les paquets sont installés séparément. Le déploiement des fichiers ne demande
ni privilèges sudo ni changement du shell de connexion.
